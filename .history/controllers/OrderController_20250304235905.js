const { Order } = require('../models');

const createOder = async (req, res) =>{
    try {
        
    } catch (err) {
        
    } finally {
        
    }
    const { totalPrice, userId } = req.body;
}